---

replicaCount: 1

image:
  repository: jtyr/exporter-test
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []

port: &container_port 80

app:
  listen: 0.0.0.0:80

env:
  SERVER_LISTEN: "{{ .Values.app.listen }}"

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

readinessProbe:
  httpGet:
    path: /healthcheck
    port: *container_port
  initialDelaySeconds: 5
  periodSeconds: 10

livenessProbe:
  httpGet:
    path: /healthcheck
    port: *container_port
  initialDelaySeconds: 15
  periodSeconds: 20

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}


service:
  type: ClusterIP
  targetPort: *container_port
  # If empty, Kubernetes will choose one for us
  port: &clusterPort 80
  labels:
    app: >-
      {{ include "this.fullname" . }}
  annotations: {}


servicemonitor:
  enabled: false
  namespace:
  release: kps


ingress:
  enabled: false
  annotations: {}
  rules:
    - paths:
        - serviceName: >-
            {{ include "this.fullname" . }}
          servicePort: *clusterPort


autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80
